file(GLOB ALL_SOURCE "*.cpp")

add_library(MATCHING_BACK_END SHARED
  ${ALL_SOURCE}
)

target_link_libraries(MATCHING_BACK_END
  ${catkin_LIBRARIES}
  PRE_INTEGRATOR
  SLIDING_WINDOW
  SENSOR_DATA
  PUBLISHER
  SUBSCRIBER
  TF_LISTENER
  TOOLS
)