file(GLOB ALL_SOURCE "*.cpp")

add_library(MAPPING_LOOP_CLOSING SHARED
  ${ALL_SOURCE}
)

target_link_libraries(MAPPING_LOOP_CLOSING
  ${catkin_LIBRARIES}
  SUBSCRIBER
  PUBLISHER
  CLOUD_FILTER
  REGISTRATION
  TOOLS
  SENSOR_DATA
)
