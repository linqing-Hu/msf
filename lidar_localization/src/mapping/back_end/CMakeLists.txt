file(GLOB ALL_SOURCE "*.cpp")

add_library(MAPPING_BACK_END SHARED
  ${ALL_SOURCE}
)

target_link_libraries(MAPPING_BACK_END
  ${catkin_LIBRARIES}
  PRE_INTEGRATOR
  GRAPH_OPTIMIZER
  SENSOR_DATA
  PUBLISHER
  SUBSCRIBER
  TF_LISTENER
  TOOLS
)
